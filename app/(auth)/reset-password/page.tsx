import { Suspense } from "react";
import ResetPasswordScreen from "../../erp/components/ResetPasswordScreen";

export default function ResetPasswordPage() {
  return (
    <Suspense>
      <ResetPasswordScreen />
    </Suspense>
  );
}
